[package]
name = "tex2math"
version = "1.0.0"
edition = "2021"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
winnow = "0.6"

[features]
default = ["debug-trace"]
debug-trace = ["winnow/debug"]
